import React from 'react';

// This screen is no longer used in the per-property sharing model.
// The app now presents a unified view of owned and shared properties directly.
const DatabaseSelectionScreen: React.FC = () => {
  return null;
};

export default DatabaseSelectionScreen;
