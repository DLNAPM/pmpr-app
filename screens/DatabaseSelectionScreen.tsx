import React from 'react';

// This component is no longer needed with the new per-property sharing model,
// as all owned and shared properties are displayed in a unified view.
// It is left here to prevent import errors but is not used.
const DatabaseSelectionScreen: React.FC = () => {
    return null;
};

export default DatabaseSelectionScreen;
