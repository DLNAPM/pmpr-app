// This screen is no longer used in the per-property sharing model.
// The main app now seamlessly integrates owned and shared properties in a single view.
const DatabaseSelectionScreen = () => {
  return null;
};
export default DatabaseSelectionScreen;